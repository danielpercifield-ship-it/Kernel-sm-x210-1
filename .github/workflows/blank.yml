name: Tab A9+ Final Victory Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🛠️ Setup Build Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y git bc build-essential flex bison libssl-dev libelf-dev clang lld llvm gcc-aarch64-linux-gnu curl dos2unix cpio zip wget

      - name: 📥 Checkout Source
        uses: actions/checkout@v4

      - name: 🔐 Integrate KernelSU
        run: |
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s v0.9.5
          
      - name: 🩹 Apply Surgical Fixes
        run: |
          # 1. 🎯 TARGETED WALT TRACE FIX (Fixes the './trace.h' fatal error)
          # This deletes the broken logic at the end and appends hard-coded paths
          sed -i '/#undef TRACE_INCLUDE_PATH/,$d' kernel/sched/walt/trace.h
          cat <<EOF >> kernel/sched/walt/trace.h
          #undef TRACE_INCLUDE_PATH
          #define TRACE_INCLUDE_PATH ../../kernel/sched/walt
          #define TRACE_INCLUDE_FILE trace
          #include <trace/define_trace.h>
          EOF

          # 2. USB-C TCPC HEADER FIX
          find drivers/usb/typec/P86801_tcpc -type f -name "*.[hc]" -exec sed -i 's|#include "inc/|#include "|g' {} + || true
          echo "subdir-ccflags-y += -Idrivers/usb/typec/P86801_tcpc/inc" >> drivers/usb/typec/P86801_tcpc/Makefile || true

      - name: 🛡️ IDENTITY CLONE & SECURITY BYPASS
        run: |
          # 🎯 FORCE THE EXACT VERSION STRING
          # Matches '5.4.249-qgki-28972655-abX210XXS6CYB9'
          sed -i 's/^EXTRAVERSION =.*/EXTRAVERSION = -qgki-28972655-abX210XXS6CYB9/' Makefile
          
          # 🚫 KILL DIRTY/PLUS TAGS
          sed -i 's/res="$res-dirty"/res="$res"/' scripts/setlocalversion
          sed -i 's/res="$res+"//' scripts/setlocalversion
          echo "" > .scmversion

      - name: ⚙️ Configure Kernel
        run: |
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- gki_stock_defconfig
          
          # 🔓 DISABLE ALL SIGNATURE/VERSION CHECKS
          # This allows your 175 patched modules to load even if the 'unique_id' is missing
          ./scripts/config --file .config --disable CONFIG_MODULE_SIG
          ./scripts/config --file .config --disable CONFIG_MODULE_SIG_ALL
          ./scripts/config --file .config --disable CONFIG_MODULE_SIG_FORCE
          ./scripts/config --file .config --disable CONFIG_MODVERSIONS
          ./scripts/config --file .config --enable CONFIG_MODULE_FORCE_LOAD
          
          # ⚡ ENABLE PREEMPTION & KSU
          ./scripts/config --file .config --set-str CONFIG_LOCALVERSION ""
          ./scripts/config --file .config --enable CONFIG_PREEMPT
          ./scripts/config --file .config --enable CONFIG_KSU
          ./scripts/config --file .config --disable LTO_CLANG
          
          yes "" | make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

      - name: 🔨 Compile Kernel
        run: |
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- LLVM=1 LLVM_IAS=1 LOCALVERSION="" KCFLAGS="-w -Wno-error" HOSTCFLAGS="-w" HOSTCXXFLAGS="-w" -j$(nproc --all) Image.gz Image modules 2>&1 | tee build.log


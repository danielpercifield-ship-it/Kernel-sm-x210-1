name: Build Tab A9+ Standard GKI (WiFi Fixed)
on:
  workflow_dispatch:

jobs:
  build-kernel:
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Prepare Environment
        run: |
          sudo apt-get update
          sudo apt-get install -y git bc build-essential flex bison libssl-dev libelf-dev clang llvm lld gcc-aarch64-linux-gnu tar zip wget python3 cpio xz-utils dos2unix curl
          
      - name: 📥 Checkout Kernel Source
        uses: actions/checkout@v4

      - name: 🩹 Apply Deep Source Code Fixes
        run: |
          # 1. CLEAN START: Remove all generated files and old configs
          make ARCH=arm64 mrproper
          
          # 2. FIX FLAG SPLITTING: This catches the "=strict-prototypes" bug by removing the illegal space
          find . -name "Makefile" -o -name "Kbuild" | xargs sed -i 's/-Werror =/-Werror=/g' || true
          find . -name "Makefile" -o -name "Kbuild" | xargs sed -i 's/-Werror  =/-Werror=/g' || true

          # 3. FIX DOS LINE ENDINGS: Stops 'unsupported character' warnings
          find . -type f -name "Kconfig*" -exec dos2unix {} + || true
          
          # 4. Strip 'q' from version strings
          find . -type f -name "gta9pwifi_eur_open_defconfig" -exec sed -i 's/qgki/gki/g' {} + || true
          sed -i 's/qgki/gki/g' Makefile || true
          
          # 5. USB-C Header Flattener
          find drivers/usb/typec -type d -name "inc" | while read -r incdir; do
            parent=$(dirname "$incdir")
            cp -r "$incdir"/* "$parent/" || true
          done
          find drivers/usb/typec -type f -exec sed -i 's|#include "inc/|#include "|g' {} +
          
          # 6. Global -Werror removal (redundancy for WERROR=0)
          find . -name "Makefile" -o -name "Kbuild" | xargs sed -i 's/-Werror//g' || true

      - name: ⚙️ Kernel Config & Hardware Force-In
        run: |
          # Load the base defconfig
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- gta9pwifi_eur_open_defconfig
          
          # FORCE BUILT-IN (Fixes WiFi and Vermagic mismatch)
          ./scripts/config --file .config -e CONFIG_WLAN \
            -e CONFIG_QCACLD_WLAN \
            -e CONFIG_WLAN_VENDOR_QCOM \
            -e CONFIG_INPUT_SENSORHUB \
            -e CONFIG_SENSORS_SSC \
            -e CONFIG_IIO
          
          # SET FAKE GKI NAME & DISABLE VERSION CHECKS
          ./scripts/config --file .config --set-str CONFIG_LOCALVERSION "-gki"
          ./scripts/config --file .config -d CONFIG_MODULE_SIG -d CONFIG_MODVERSIONS
          
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

      - name: 🔨 Compile
        run: |
          echo "-28972655-abX210XXS6CYB9" > .scmversion
          
          # WERROR=0 is the primary fix to stop warnings from acting as errors
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- \
            LLVM=1 LLVM_IAS=1 HOSTCC=gcc CC=clang \
            WERROR=0 \
            -j$(nproc --all) Image 2>&1 | tee build.log

      - name: 📦 Package Output
        if: success()
        run: |
          if [ -f arch/arm64/boot/Image ]; then
            git clone https://github.com
            sed -i 's/do.devicecheck=1/do.devicecheck=0/g' AnyKernel3/anykernel.sh
            cp arch/arm64/boot/Image AnyKernel3/
            cd AnyKernel3 && zip -r9 ../TabA9Plus-GKI-Pure-Fixed.zip *
          else
            echo "Error: Kernel Image (arch/arm64/boot/Image) not found!"
            exit 1
          fi

      - name: 📤 Upload Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: TabA9Plus-GKI-Fixed
          path: TabA9Plus-GKI-Pure-Fixed.zip
